<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:gravity="center|top"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <EditText
            android:id="@+id/original_password"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="40dp"
            android:layout_margin="10dp"
            android:inputType="numberPassword"
            android:background="@drawable/password_edit_round"
            android:drawableLeft="@drawable/old_password"
            android:hint="原密码"/>

        <EditText
            android:id="@+id/new_password"
            android:layout_width="match_parent"
            android:layout_weight="1"
            android:layout_height="40dp"
            android:layout_margin="10dp"
            android:inputType="numberPassword"
            android:background="@drawable/password_edit_round"
            android:drawableLeft="@drawable/new_password"
            android:hint="新密码"/>

    </LinearLayout>

    <Button
        android:id="@+id/modify"
        android:layout_width="match_parent"
        android:background="@drawable/password_button_round"
        android:layout_height="wrap_content"/>
</LinearLayout>